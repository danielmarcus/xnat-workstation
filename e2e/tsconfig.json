{
  "compilerOptions": {
    "target": "ES2020",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "outDir": "../dist/e2e",
    "rootDir": "..",
    "baseUrl": "..",
    "paths": {
      "@shared/*": ["src/shared/*"]
    },
    // Specs run Playwright in Node, but page.evaluate() callbacks are compiled here
    // and execute in the RENDERER — they legitimately use window/document/getComputedStyle.
    "lib": ["ES2020", "DOM", "DOM.Iterable"]
  },
  "include": [
    "./**/*.ts",
    "../src/shared/types/**/*.ts"
  ],
  "exclude": ["node_modules"]
}
